<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.midnight.sindicato">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sindicato">
<!--        <activity-->
<!--            android:name=".CertificateViewActivity"-->
<!--            android:exported="false" />-->
        <activity
            android:name=".ui.voucher.VoucherActivity"
            android:exported="false"
            android:label="@string/title_activity_voucher"
            android:theme="@style/Theme.Sindicato.NoActionBar" />
        <activity
            android:name=".ui.news.NewsActivity"
            android:exported="false"
            android:label="@string/title_activity_news"
            android:theme="@style/Theme.Sindicato.NoActionBar" />
        <activity
            android:name=".ui.certificate.CertificateActivity"
            android:exported="false"
            android:label="@string/title_activity_certificate"
            android:theme="@style/Theme.Sindicato.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:parentActivityName=".ui.login.LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.login.LoginActivity" />
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>